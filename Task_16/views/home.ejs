<!DOCTYPE html>
<html>
<head>
  <title>Google Login</title>
</head>
<body>
  <% if (user) { %>
    <h2>Welcome, <%= user.displayName %>!</h2>
    <p>Email: <%= user.emails[0].value %></p>
    <img src="<%= user.photos[0].value %>" alt="Profile Photo" width="100">
    <br><br>
    <a href="/logout">Logout</a>
  <% } else { %>
    <h2>Please log in</h2>
    <a href="/auth/google">Login with Google</a>
  <% } %>
</body>
</html>
